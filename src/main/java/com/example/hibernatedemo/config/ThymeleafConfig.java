package com.example.hibernatedemo.config;

/**
 * External frontend resolver removed — templates are now served from the classpath
 * (src/main/resources/templates) and static assets from classpath:/static.
 * This class left intentionally blank to keep historical reference.
 */
public class ThymeleafConfig {
    // intentionally empty: using Spring Boot's default Thymeleaf auto-configuration
}
